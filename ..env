# Signaling Server URL
# The WebSocket URL for the signaling server (Socket.IO)
# Default: http://localhost:3000
VITE_SIGNALING_SERVER_URL=https://anydesk-clone.onrender.com

# TURN Server Configuration (Optional)
# Required for NAT traversal when peers cannot connect directly
# Only needed if users are behind firewalls/NAT
# 
# TURN Server URL format: turn:your-turn-server-ip:3478
# VITE_TURN_SERVER_URL=turn:your-turn-server-ip:3478
# VITE_TURN_USERNAME=your-turn-username
# VITE_TURN_PASSWORD=your-turn-password

# Environment
# Set to 'production' for production builds
NODE_ENV=development
